trivial:
  - CHANGELOG.md is now generated by ``antsibull-changelog`` from fragments rather than
    edited by hand; CI lints the changelog and fails when the generated file drifts from
    ``changelogs/changelog.yaml``, and the release workflow renders its GitHub release body
    from the same source. Repository tooling only - nothing a consumer of the collection
    calls has changed.
