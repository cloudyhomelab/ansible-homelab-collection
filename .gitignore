__pycache__/
*.pyc
.pytest_cache/
# The development tools' virtual environment (see README, Tests).
.venv/
# Built collection artifacts and whatever ansible-lint or molecule leave behind.
*.tar.gz
.ansible/
.cache/
# Written by `antsibull-changelog release` to spot new plugins; `generate` and CI never read it.
changelogs/.plugin-cache.yaml
.DS_Store

.idea/
docs/
